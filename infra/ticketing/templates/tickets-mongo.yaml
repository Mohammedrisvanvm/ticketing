apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ticketing.fullname" . }}-tickets-mongo
  labels:
    app: tickets-mongo
    {{- include "ticketing.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.ticketsMongo.replicaCount }}
  selector:
    matchLabels:
      app: tickets-mongo
  template:
    metadata:
      labels:
        app: tickets-mongo
        {{- include "ticketing.labels" . | nindent 8 }}
    spec:
      containers:
        - name: tickets-mongo
          image: "{{ .Values.ticketsMongo.image.repository }}:{{ .Values.ticketsMongo.image.tag }}"

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticketing.fullname" . }}-tickets-mongo-srv
spec:
  selector:
    app: tickets-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
